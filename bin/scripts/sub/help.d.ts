import { EFramework } from '../../types';
/**
 * Help script for the CLI
 * @param framework Framework to use
 * @returns void
 */
export declare const useHelpScript: (framework?: EFramework) => void;
